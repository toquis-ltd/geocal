export enum NumberOfTranfromationsEnum{
    One,
    Two,
    Three,
}

export enum ResultFormatEnum{
    DD = 'DD',
    DD_MM = 'DD MM',
    DD_MM_SS = 'DD MM SS',
    DDdnMM = 'DD-MM',
    DDdnMMdnSS = 'DD-MM-SS',
    // DDdtMM = 'DD.MM',
}

export enum FileFormatEnum{
    shp = 'shp',
    csv  = 'csv',
    geojson = 'geojson',
    kml  = 'kml',
    gml = 'gml',
    gpx = 'gpx',
}